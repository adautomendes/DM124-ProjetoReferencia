apiVersion: v1
kind: Service
metadata:
  name: {{ include "petstore.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "petstore.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
