apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitor.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "monitor.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
